"use strict";
let canal = 'Gaveta';
let inscritos = 12312;
// canal = inscritos
console.log(`canal = ${canal}`);
let nome = 'Pedro batata';
console.log(`nome = ${nome}`);
const soma = (a, b) => a + b;
function sayHi(isMorning) {
    let hiho;
    if (isMorning) {
        hiho = 'good morning';
    }
    else {
        hiho = 'good night';
    }
    return hiho;
}
